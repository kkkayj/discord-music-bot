[phases.setup]
nixPkgs = ["ffmpeg", "curl"]

[phases.build]
cmds = [
  "curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o yt-dlp-linux && chmod +x yt-dlp-linux"
]

[start]
cmd = "node index.js"
